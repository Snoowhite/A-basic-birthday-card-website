# A-basic-birthday-card-website
A simple birthday card skeleton website built with basic HTML and CSS.

In this project, I practiced using several CSS properties, pseudo-classes, and pseudo-elements such as:

- `:hover`
- `:active`
- `:focus`
- `::before`
- `::after`

I also used basic HTML elements like:
- `div`
- `section`
